import java.util.Arrays;

public class Controller {

    public static void main(String[] args){

        Heap heap = new Heap();
        heap.MinHeapify(heap.array, 2);
        System.out.println(heap);

        heap.buildMinHeap(heap.array);
        System.out.println(heap);

    }
}
